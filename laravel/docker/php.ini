; OPcache/JIT 以外のベース設定。
; OPcache と JIT の値は start-container.sh が環境変数から
; zzz-perf.ini を生成して上書きする（リビルド不要で切替可能）。

memory_limit = 512M
realpath_cache_size = 4096K
realpath_cache_ttl = 600

; 計測時にエラー表示でノイズが出ないように
display_errors = Off
log_errors = On
